# Candidate B (selective routed execution) — DEAD (2026-06-02)
Premise test (shared_only_argmax.py, Qwen, 3935 positions teacher-forced): dropping ALL routed experts (shared+attention only) changes the next-token argmax for 95.3% of tokens (only 4.7% agree). Routed experts are a ~10% hidden-norm perturbation BUT the LM head is sensitive -> 95% of argmax decisions need routed. So you CANNOT skip routed fetch for most tokens. B is dead.
(margin keep 0.22 vs flip 0.066 -> shared-only confidence weakly separates, but base flip rate 95% kills any savings.)
IMPLICATION: routed experts MUST be computed (can't skip) -> the only lever is making their movement/computation CHEAPER -> Candidate A (compute-placement: CPU-compute routed, move 8KB activations not 17MB weights; CPU DRAM 100GB/s >> PCIe; ~2000x data asymmetry). A is the surviving 'better way'. codex pressure-testing A vs Fiddler/HybriMoE/ktransformers.

## codex better-way verdict (2026-06-02): A=Fiddler (not non-incremental), B=dead, third idea=cross-request multiplexing
- A (CPU-compute on miss): NOT non-incremental — Fiddler(2402.07033)/HybriMoE/KTransformers already CPU-compute MoE experts to avoid PCIe weight movement. SPICE adds only a placement hint. Exact-semantics layer dependency: CPU routed output must merge before next layer (can't hide behind downstream GPU attention losslessly). Strong practical system, incremental principle.
- B (shared-only/uncertainty-gated): dead (my shared_only_argmax = 4.7% agree; need >=90%).
- codex third idea: cross-request expert multiplexing (layer-synchronous decode batching; each unique expert fetched/CPU-computed once for many streams; uniformity -> coupon-collector amortization; exact, no drop). Needs batch>1 serving boundary. Decisive: 8/16/32/64 streams, GO if B16-32 gives >2x bytes/token AND >1.5x TPOT under SLO.
- BRUTAL bottom line (triple-confirmed: my experiments + codex strategic + codex better-way): batch=1 single-stream offloaded-MoE caching/prediction is STRUCTURALLY EXHAUSTED; no non-incremental mechanism. Honest contributions: (I) negative characterization paper [non-incremental in kind], (II) practical CPU-compute system [incremental, Fiddler-regime], (III) pivot to cross-request serving multiplexing [needs batch>1]. SCOPE DECISION belongs to the user.
